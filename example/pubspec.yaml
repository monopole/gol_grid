name: golapp
description: Demo of gol_grid.
publish_to: none

version: 1.2.0

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  grid_world: ^2.0.0
  gol_grid:
    path: ../
  thumper: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^1.0.1

flutter:
  uses-material-design: true
