name: golapp
description: Demo of gol_grid.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  grid_world: ^1.0.1
  gol_grid:
    path: ../
  thumper: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
