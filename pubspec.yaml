name:  gol_grid
description: A widget showing a controllable square grid of cellular automata.
version: 0.1.7
homepage: https://github.com/monopole/gol_grid

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^3.2.0
  grid_world: ^1.0.1
  test: ^1.14.2
  thumper: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
